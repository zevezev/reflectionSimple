// export class Constants {
//   static roomWidth = 400;
//   static roomHeight = 400;
//   static roomX = window.innerWidth / 2 - this.roomWidth / 2;
//   static roomY = window.innerHeight / 2;

//   static diamondX = this.roomX + (this.roomWidth * 3) / 4;
//   static diamondY = this.roomY + (this.roomHeight * 1) / 2;

//   static viewerX = this.roomX + this.roomWidth / 4;
//   static viewerY = this.roomY + (this.roomHeight * 1) / 2;

//   static diamondRadius = 40;
//   static mirrorDiamondX = this.diamondX;
//   static mirrorDiamondY =
//     this.roomY - (this.diamondY - this.roomY) - this.diamondRadius / 2;
// }
